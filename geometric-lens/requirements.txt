numpy>=1.26
xgboost>=2.0
scikit-learn>=1.3   # G(x) trainer: train/test split + metrics (atlas lens build)
safetensors>=0.4    # optional: pickle-free C(x) twin written alongside cost_field.pt
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3
httpx==0.26.0
pyyaml==6.0.1
python-multipart==0.0.6
# tiktoken: unused in V2 (was used for V1 chunking token counting). Retained as
# it is a transitive dependency that may be needed by future features.
tiktoken==0.5.2
redis>=5.0.0
tree-sitter>=0.23.0
tree-sitter-python>=0.23.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.10.0
